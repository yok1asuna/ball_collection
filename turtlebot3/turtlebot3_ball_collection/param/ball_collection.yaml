ball_detector_node:
  ros__parameters:
    publish_rate: 1.0  # Hz

clustering_node:
  ros__parameters:
    eps: 1.0  # DBSCAN epsilon
    min_samples: 2  # DBSCAN min samples

tsp_planner_node:
  ros__parameters:
    # TSP parameters if needed

path_planner_node:
  ros__parameters:
    # Navigation parameters if needed
